============
Installation
============

At the command line::

    $ easy_install oshi_rest_server

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv oshi_rest_server
    $ pip install oshi_rest_server
